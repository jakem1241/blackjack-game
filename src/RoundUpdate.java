public class RoundUpdate {

    /*
    
    class RoundUpdate {
        String message               // What the UI should display
        boolean needsPlayerInput     // Should ask for another "draw?" input
        boolean roundEnded           // Is the round over?
        int result                   // 1 = win, 0 = tie, -1 = loss

        constructor(message, needsInput, roundEnded, result)
        getters
    }

    */
}
