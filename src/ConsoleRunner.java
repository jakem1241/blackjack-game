public class ConsoleRunner {

    /*
    
    class ConsoleRunner {
        main() {
            create Scanner
            create GameContext using scanner

            print welcome message and starting stats

            while (money > 0 and lives > 0) {
                gtx.initRound()
                RoundEnvironment renv = new RoundEnvironment(gtx)

                if (player has All or Nothing & enough points) {
                    ask if player wants to activate All or Nothing
                    if yes:
                        renv.startRound(-1)
                        result = renv.playRound(true)
                        update lives based on result
                        print result message
                        continue
                }

                wager = get wager from user (or handle "exit")
                renv.startRound(wager)
                result = renv.playRound(true)
                update gtx money/lives based on result
                print round outcome
            }

            print game over message
        }
    }

    */
}
